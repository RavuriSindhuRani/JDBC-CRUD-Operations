# JDBC-CRUD-Operations
JDBC CRUD Operations
The Library Management System heavily depends on CRUD operations—Create, Read, Update, Delete.
These operations are implemented using JDBC (Java Database Connectivity) to interact with the MySQL database.
